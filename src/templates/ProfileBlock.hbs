<div class="profile-info">
    <img src="{{this.profileData.avatar}}">
    <div class="details">
        <h2>{{this.profileData.username}}</h2>
        <p>{{this.profileData.bio}}</p>
    </div>
</div>
{{#if this.isOwn}}
<button id="profile-edit-button">Редактировать профиль</button>
{{/if}}